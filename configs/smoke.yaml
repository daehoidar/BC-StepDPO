# Smoke test config — M1 Pro + Qwen2.5-0.5B + 18 rows
sft:
  use_lora: true
  lora_r: 8
  lora_alpha: 16
  lora_targets: ["q_proj", "v_proj"]
  epochs: 1
  batch_size: 1
  grad_accum: 2
  max_len: 512
  lr: 2.0e-5
  warmup_steps: 2
